server:
  port: 8097

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MerlyGarcia/config-repo.git
          username: MerlyGarcia
          password: MerlyFabiola1823
          default-label: master
  profiles:
    active: git
